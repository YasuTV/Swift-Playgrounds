while !isOnGem {
    if isBlocked && isBlockedRight {
        turnLeft()
    } else if isBlockedRight{
        moveForward()
    } else if isBlocked {
        turnRight()
        moveForward()
    } else {
        turnRight()
        moveForward()
    }
}

collectGem()
